/* pages/posnet/index.wxss */
.camera {
  width: 100vw;
  z-index: 5;
}

.canvas {
  width: 100%;
  height: 100%;
  z-index: 10;
}

/* .result {
  position: absolute;
  top: 50%;
  left: 50%;
  font-size: xx-large;
  z-index: 15;
} */


.result {
  position: fixed;
  bottom: 10rpx; /* 将视图固定在屏幕底部 */
  margin-left: 120rpx;
  font-size: 10vw;
  width: 100vw;
  color: white; /* 字体颜色设置为白色 */
  text-shadow: 2px 2px 2px black; /* 添加黑色描边，可根据需要调整偏移量 */
  z-index: 15;
}
